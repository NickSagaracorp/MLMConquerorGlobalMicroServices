# ── Claude memory & config ─────────────────────────────────────────────────────
.claude/

# ── Markdown — exclude all .md except README.md y los specs de diseño ─────────
*.md
!README.md
!docs/superpowers/specs/*.md
!docs/superpowers/plans/*.md

# ── Specs de diseño — el .md es la fuente; el .pdf se genera a partir de él ────
docs/superpowers/specs/*.pdf

# ── Visual Studio ─────────────────────────────────────────────────────────────
.vs/
*.suo
*.user
*.userosscache
*.sln.docstates
*.userprefs
*.pidb
*.booproj
*.svd
*.pdb
*.opensdf
*.sdf
*.cachefile
*.VC.db
*.VC.VC.opendb

# Visual Studio Code
.vscode/

# Rider
.idea/
*.sln.iml

# ReSharper
_ReSharper*/
*.[Rr]e[Ss]harper
*.DotSettings.user

# NCrunch
*.ncrunch*
.*crunch*.local.xml
nCrunchTemp_*

# ── .NET / C# ──────────────────────────────────────────────────────────────────
bin/
obj/
*.nupkg
*.snupkg
project.lock.json
project.fragment.lock.json
artifacts/
ScaffoldingReadMe.txt

# MSTest
TestResults/
[Tt]est[Rr]esult*/
*.trx

# Coverage
coverage/
*.coveragexml
*.coverage
*.coveragesettings
CoverageReport/

# Publish
publish/
PublishProfiles/
**/Properties/PublishProfiles/

# NuGet
*.nuget.props
*.nuget.targets
packages/
!**/packages/build/
nuget.config

# Entity Framework migrations snapshot (keep .cs, exclude designer cache)
*.edmx.diagram

# Blazor / MAUI
*.maui-*

# Windows App packaging
AppPackages/
BundleArtifacts/
Package.StoreAssociation.xml
_pkginfo.txt
*.appx
*.appxbundle
*.appxupload

# ── Secrets & environment ──────────────────────────────────────────────────────
appsettings.*.json
!appsettings.json
*.pfx
*.p12
*.pem
*.key
*.cer
*.crt
secrets.json
.env
.env.*
!.env.example

# ── OS ────────────────────────────────────────────────────────────────────────
.DS_Store
Thumbs.db
desktop.ini

# ── Logs ──────────────────────────────────────────────────────────────────────
*.log
logs/

# ── Large / binary assets (use Git LFS or external storage) ───────────────────
*.zip
*.rar
*.7z

# ── Generated rank certificates (artifacts, not source) ───────────────────────
MLMConquerorGlobalEdition.RankEngine/wwwroot/certificates/*.pdf

# MemPalace per-project files (issue #185)
mempalace.yaml
entities.json
