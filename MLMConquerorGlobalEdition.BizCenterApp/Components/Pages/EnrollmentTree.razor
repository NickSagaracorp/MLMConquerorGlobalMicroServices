@page "/team/enrollment"
@attribute [Authorize(Roles = "Ambassador,ExternalMember")]
@inject NavigationManager Nav

@code {
    protected override void OnInitialized()
        => Nav.NavigateTo("/team/enrollment/my-team", replace: true);
}
