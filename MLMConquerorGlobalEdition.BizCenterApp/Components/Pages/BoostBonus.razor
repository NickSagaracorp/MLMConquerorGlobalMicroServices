@page "/commissions/boost"
@rendermode @(new InteractiveAutoRenderMode(prerender: false))
@attribute [Microsoft.AspNetCore.Authorization.Authorize(Roles = "Ambassador")]
@inject IViewContextService ViewContext

<PageTitle>Boost Bonus</PageTitle>

<BoostBonusReport MemberProfileId="@ViewContext.ViewingMemberId" />
